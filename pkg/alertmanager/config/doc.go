package config

// Copied over from https://github.com/prometheus/alertmanager/tree/master/config as those are internal types with a few adjustments:
// - The secret now returns the actual secret value instead of <secret>
