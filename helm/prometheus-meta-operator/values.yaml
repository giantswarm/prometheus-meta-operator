image:
  name: "giantswarm/prometheus-meta-operator"
  tag: "[[ .Version ]]"

registry:
  domain: quay.io

pod:
  user:
    id: 0
  group:
    id: 1000

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

alertmanager:
  logLevel: info

prometheus:
  logLevel: info
